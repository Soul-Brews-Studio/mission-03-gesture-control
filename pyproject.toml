[project]
name = "mission-03-gesture-control"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.8.0",
    "mediapipe>=0.10.0",
    "paho-mqtt>=2.0.0",
]

[tool.uv]
dev-dependencies = []
